# Task 1.2: .env.example 同步新增配置项

## 状态
DONE

## 实现内容摘要
在 `.env.example` 末尾新增三组环境变量配置，与 `config.py` 新增字段一一对应：

- **LLM 通用配置（8 项）**: `LLM_BASE_URL`, `LLM_API_KEY`, `LLM_MODEL`, `LLM_REQUEST_TIMEOUT`, `LLM_CONNECT_TIMEOUT`, `LLM_MAX_CONCURRENCY`, `LLM_MAX_RETRIES`, `LLM_RETRY_BACKOFF_BASE`
- **Rerank 配置（9 项）**: `RERANK_ENABLED`, `RERANK_CANDIDATE_MULTIPLIER`, `RERANK_MIN_CANDIDATES`, `RERANK_MAX_CANDIDATES`, `RERANK_CHUNK_MAX_CHARS`, `RERANK_TEMPERATURE`, `RERANK_CACHE_TTL_SECONDS`, `RERANK_CACHE_MAX_ENTRIES`, `RERANK_CACHE_SIMILARITY_THRESHOLD`
- **Rewrite 配置（5 项）**: `REWRITE_ENABLED`, `REWRITE_TEMPERATURE`, `REWRITE_CACHE_TTL_SECONDS`, `REWRITE_CACHE_MAX_ENTRIES`, `REWRITE_CACHE_SIMILARITY_THRESHOLD`

每组前添加中文注释说明用途，组间空行分隔，与现有格式风格一致。

## 提交哈希
`bde20826a83b6177afb8bb64cebdf74429aa883f`

## 变更文件列表
- `.env.example` (+28 行)
